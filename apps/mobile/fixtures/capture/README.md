# Capture fixtures

Fixtures in this directory are capture-session metadata and replay inputs only. They do not include credentials or production media.

`phase-04-single-device.json` describes the minimum physical-device capture proof. Add sanitized event traces and media object references here after a real-device run; retain the original Egress object ID and any derived recovery version ID.
